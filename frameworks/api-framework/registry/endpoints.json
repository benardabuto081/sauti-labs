{
  "$comment": "Central registry of all API endpoints across the Sauti Labs platform. Each entry must conform to frameworks/api-framework/schema/api-endpoint.schema.json. This is the source of truth for what capabilities are exposed to developers, and which models and products depend on them.",
  "endpoints": []
}
