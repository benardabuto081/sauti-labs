{
  "$comment": "Central registry of all annotation tasks across the Sauti Labs platform. Each entry must conform to frameworks/annotation-framework/schema/annotation-task.schema.json. Tracks the human and AI-assisted annotation process as an auditable step between raw data and validated datasets.",
  "tasks": []
}
