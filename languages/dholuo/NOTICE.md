# Licensing & Provenance Notice

This language program was generated from the Sauti Labs
`language-program-framework` template.

The metadata file (`language.meta.json`) and this directory structure
are original Sauti Labs infrastructure, covered by the repository's
Apache License, Version 2.0 (see `/LICENSE` and `/NOTICE` at the
repository root).

**The actual language content placed inside `corpus/`, `speech/`,
`lexicon/`, and `benchmarks/` is NOT automatically covered by that
license.** Every source added to this language program must be
registered in `frameworks/corpus-registry` and/or
`frameworks/speech-registry` with an explicit, honestly-set
`copyright_status`, and retains whatever license its original source
was published under.

See `languages/LICENSE-DATA.md` at the root of the `languages/`
directory for the full explanation of this licensing boundary, and
`docs/architecture/ADR-0003.md` for the architectural reasoning.

**Do not assume content in this directory is freely reusable until
you have checked its specific registry entry.**